# تهيئة قاعدة بيانات Mido

يدعم معالج التهيئة الاتصال بقاعدة موجودة بدل افتراض إنشاء قاعدة جديدة. الأولوية الحالية هي Oracle عبر `oracledb`، مع دعم اختبارات الاتصال ومسار التهيئة الموجود لمحركات SQL Server وAccess.

## Oracle

أدخل المضيف والمنفذ واسم الخدمة واسم المستخدم وكلمة المرور. الاتصال الافتراضي هو `127.0.0.1:1521/XEPDB1`، ويمكن تغييره من شاشة التهيئة. استخدم حسابًا يملك صلاحيات إنشاء الجداول والكائنات عند تشغيل migrations لأول مرة، ثم استخدم حساب التطبيق الأقل صلاحية للتشغيل اليومي.

بعد نجاح اختبار الاتصال يجري النظام فحصًا للجداول الأساسية ويعرض الجداول الناقصة. إذا كان المخطط جاهزًا يمكن الاتصال به مباشرة. إذا كانت هناك جداول ناقصة، استخدم زر التهيئة لتشغيل ترحيلات Oracle بالترتيب، بما فيها دورة المستندات والتدقيق.

> لا تُخزّن كلمة مرور Oracle في الواجهة. مرّرها عبر قناة الإعداد الآمنة، ولا تضعها في Git أو ملفات `.env` المرفوعة.

## الاتصال الموجود مقابل إنشاء مخطط جديد

الاتصال الموجود هو المسار المفضل. التهيئة لا تستبدل البيانات الموجودة تلقائيًا، لكن يجب أخذ نسخة احتياطية واختبار الترحيلات على نسخة اختبار قبل تشغيلها على الإنتاج. فحص الجاهزية يقرأ جداول المستخدم الحالي؛ لذلك يجب أن يكون الحساب متصلًا بالخدمة الصحيحة، مثل `XEPDB1`، لا بالحاوية الجذرية التي لا تحتوي على جداول التطبيق.

## SQL Server وAccess

مسار SQL Server يستخدم `mssql` ومسار Access يستخدم ODBC. التهيئة المحمولة الحالية تنشئ المخطط الأساسي المتوافق، لكنها لا تدّعي تطابق كل ميزات Oracle، مثل triggers الخاصة بدورة المستندات وسجل التدقيق غير القابل للتعديل. يجب تنفيذ طبقة توافق مستقلة قبل اعتماد SQL Server أو Access كبديل إنتاجي كامل.

## اختبار ما بعد التهيئة

بعد التهيئة، اختبر تسجيل الدخول، اختيار الشركة والفرع والسنة المالية، إنشاء صنف وجهة، إنشاء أمر بيع أو شراء، تحويله إلى إذن تسليم أو استلام، إنشاء الفاتورة، ثم التحقق من ظهور الحدث في سجل التدقيق. لا يُعتمد الاتصال للإنتاج قبل نجاح هذه الدورة على نسخة اختبار.
