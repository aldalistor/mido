# نسخة أونكس ERP — Microsoft Access

هذه النسخة مستقلة عن المستودع الأصلي، ومكانها `/home/ubuntu/mido-erp-access`. لم يتم تعديل `/home/ubuntu/mido`.

## النطاق

تحتفظ النسخة بكل وحدات أونكس الحالية: المحاسبة العامة، القيود، المبيعات والمشتريات، المخزون، العملاء والموردون، الصندوق والبنوك، المصروفات والإيرادات، التقارير، إعادة تقييم العملات، إقفال السنة والصلاحيات. كما أضيف مسار Access مخصص لسطح مكتب Windows باستخدام ODBC.

## تشغيل Windows

```powershell
npm install
npm start
```

لإنشاء مثبت Windows:

```powershell
npm run build:win
```

## Access

من شاشة **تهيئة قاعدة البيانات** اختر **Microsoft Access**، ثم أدخل مسار ملف `.accdb`، واضغط **اختبار الاتصال**. بعد نجاح الاختبار اضغط **حفظ والانتقال للتهيئة** لإنشاء جداول ERP الأساسية. زر **نسخة احتياطية** ينسخ ملف Access قبل التغييرات.

يجب تثبيت **Microsoft Access Database Engine 2016 Redistributable** وبنفس معمارية التطبيق. يوصى باستخدام Access Database Engine 64-bit مع نسخة Electron/Windows 64-bit. لا تحفظ كلمة مرور أو سلسلة اتصال حساسة داخل Git.

## تعدد العملات

يحتوي مخطط Access على جداول العملات وأسعار الصرف، مع عملة أساس `SAR` وعملات أولية `USD` و`YER`. تحفظ الفاتورة والقيد العملة الأصلية وسعر الصرف والقيمة المحولة إلى العملة الأساسية. يجب اعتماد أسعار الصرف اليومية قبل الترحيل، وتُنفذ إعادة التقييم من شاشة العملات الأجنبية.

## ملاحظة هندسية

Access مناسب لبيئة مكتبية صغيرة أو متوسطة مع ملف مركزي على شبكة موثوقة وعدد مستخدمين محدود. للبيئات متعددة الفروع أو الاستخدام المتزامن المكثف يوصى بالترقية لاحقًا إلى SQL Server أو Oracle مع إبقاء الواجهة نفسها.

## التحقق

```bash
npm test
node --check access-db.js
```

هذه النسخة هي نقطة تطوير مستقلة، ويمكن لاحقًا ربط عمليات القراءة والكتابة اليومية في الوحدات المختلفة بطبقة Access نفسها بدل طبقة Oracle الحالية.
