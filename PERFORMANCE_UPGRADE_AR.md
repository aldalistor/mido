# تحديثات الأداء والجاهزية التشغيلية

## ما تم تنفيذه

أضيفت Migration رقم `017_performance_indexes.sql` وتحتوي على فهارس مركبة للاستعلامات الأكثر استخدامًا في العملاء والأصناف والفواتير والمستندات والقيود والمخزون وسجل التدقيق والدفعات والفترات المالية. الترحيل آمن عند إعادة التشغيل؛ إذ يتجاهل الفهرس إذا كان موجودًا بالفعل.

أصبحت قوائم العملاء والأصناف والفواتير والمستندات التجارية تدعم الترقيم والبحث من الخادم. السلوك القديم محفوظ عند عدم إرسال `page` أو `pageSize`، بينما يعيد المسار الجديد كائنًا يحتوي على `rows` و`total` و`hasMore`. الحد الأقصى لحجم الصفحة هو 500 سجل.

أضيفت قناة `db:global-search` للبحث الموحد في العملاء والأصناف والفواتير، مع حد أدنى لحرفين وحد أقصى للنتائج لتجنب تحميل بيانات غير ضرورية إلى الواجهة.

أضيف Cache قصير العمر لمؤشرات لوحة التحكم مع مفتاح يعتمد على سياق الشركة والفرع والسنة المالية، كما أضيفت دالة `invalidateDashboardCache` للاستخدام عند الحاجة إلى إبطال النتائج بعد عمليات الكتابة.

أضيف حارس مركزي للفترة المالية يمنع إنشاء القيود وترحيل الفواتير وترحيل الدفعات عندما لا توجد فترة تغطي تاريخ العملية أو عندما تكون الفترة مغلقة أو تحت الإقفال.

أضيفت `performance-utils.js` التي توفر الترقيم، ونتيجة الصفحات، وCache بزمن انتهاء، وDebounce، وقياسًا زمنيًا للعمليات. كما أضيف اختبار وحدات لها إلى `npm test`.

## التشغيل

بعد تشغيل Oracle وتطبيق الترحيلات السابقة، نفّذ الترحيل الجديد بحساب `ONYX_APP`:

```bash
sqlplus ONYX_APP/<password>@127.0.0.1:1521/XEPDB1 @oracle/migrations/017_performance_indexes.sql
```

ثم شغّل اختبارات المشروع:

```bash
npm test
```

## عقدة الترقيم الجديدة

يمكن استدعاء القوائم بهذه الصورة:

```js
window.onyxAPI.listInvoices({
  search: 'INV-2026',
  page: 2,
  pageSize: 50,
  invoiceType: 'SALE',
  statusCode: 'POSTED'
});
```

وتكون النتيجة:

```js
{
  rows: [],
  page: 2,
  pageSize: 50,
  total: 1240,
  hasMore: true
}
```

## عناصر تحتاج تحققًا خارجيًا

لا يمكن إثبات خطة الاستعلامات أو زمن التقارير على بيانات فعلية دون اتصال Oracle يحتوي على حجم إنتاجي. يجب تشغيل `EXPLAIN PLAN` على التقارير، ومقارنة أزمنة الاستعلام قبل وبعد Migration 017.

كما يجب اختبار إنشاء حزمة Windows وتثبيتًا نظيفًا، واختبار النسخ الاحتياطي والاستعادة على بيئة تشغيل فعلية. ملفات النسخ الاحتياطي الموجودة في `oracle/server` مخصصة لتشغيل الخادم وتحتاج أسرار البيئة وصلاحيات Docker/Oracle؛ لذلك لم تُشغّل داخل بيئة الفحص.

## توصيات المرحلة التالية

ينبغي ربط الترقيم بمكونات الجداول في `renderer.js` بدل استخدام قوائم كاملة، وإضافة Virtual Scrolling للجداول الكبيرة. كما ينبغي تشغيل التقارير والتصدير في Worker، وإضافة شاشة تنبيهات للمخزون والذمم، ثم إضافة تصدير CSV/Excel/PDF مع اختبار تدفق البيانات.
