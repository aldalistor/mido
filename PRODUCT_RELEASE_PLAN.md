# خطة تحويل أونكس إلى منتج تجاري

## تعريف المنتج

أونكس هو نظام محاسبي مكتبي عربي بواجهة RTL يستهدف الشركات الصغيرة والمتوسطة التي تحتاج إلى المحاسبة العامة، العملاء والموردين، المبيعات والمشتريات، المخزون، التقارير، المستخدمين والصلاحيات، والعمل على خادم Oracle مركزي داخل شبكة العميل.

| محور المنتج | القرار المبدئي |
|---|---|
| المنصة | Windows 10/11 64-bit، مع اختبار Linux للتطوير فقط |
| نمط التشغيل | تطبيق Electron على أجهزة المستخدمين، Oracle على خادم العميل |
| اللغة | العربية RTL مع قابلية إضافة الإنجليزية |
| العميل المستهدف | شركات تجارة وخدمات متعددة المستخدمين والفروع |
| البيانات | قاعدة منفصلة لكل عميل أو Schema مستقل مع عزل واضح |
| التفعيل | ترخيص مرتبط بالعميل والخادم وعدد المستخدمين والفروع |
| الدعم | نسخ احتياطية، سجل تدقيق، تشخيص اتصال، وتحديثات مرقمة |

## الحزمة التجارية المضافة

تتضمن النواة التجارية الحالية ترخيصًا موقّعًا قابلًا للتحقق بالمفتاح العام، حالات `ACTIVE` و`GRACE` و`EXPIRED` و`REVOKED`، حدودًا للمستخدمين والفروع، وصلاحيات عملية للأدوار الأساسية. كما تتضمن نواة فاتورة تتحقق من البيع والشراء، الخصومات، الضريبة، النقدي والآجل والجزئي، الرصيد المتبقي، والمرتجعات الجزئية. هذه النواة لا تغني عن اختبار قاعدة Oracle، واجهة المستخدم، التثبيت، والاعتماد المحاسبي مع عميل فعلي.

## الحد الأدنى القابل للبيع

لن يُعتبر الإصدار جاهزاً للبيع إلا بعد أن يستطيع العميل تثبيت التطبيق، إنشاء شركة وفرع وسنة مالية، إنشاء أول مدير، إضافة المستخدمين والأدوار، تعريف دليل الحسابات، إنشاء جهة وصنف، تسجيل فاتورة وقيد، استخراج التقارير الأساسية، أخذ نسخة احتياطية واستعادتها، وتفعيل الترخيص دون تدخل برمجي مباشر.

## ما يجب فصله عن النسخة التطويرية

بيانات الاختبار الحالية مثل `ADMIN_TEST` والعميل والصنف التجريبيين لا تدخل في إصدار العميل. كما يجب فصل ONYX_APP لكل عميل أو إنشاء آلية tenant واضحة، وعدم استخدام Schema الترحيل القديمة كطبقة تشغيل. جميع الأسرار ومفاتيح التفعيل يجب أن تبقى خارج Git، وتُولد أثناء التثبيت أو عبر بوابة الترخيص.

## بوابة الاعتماد التجاري

قبل البيع نحتاج إلى اختبار تثبيت نظيف على Windows، اختبار اتصال Oracle من جهاز عميل، اختبار التحديث والاستعادة، اختبار صلاحيات كل دور، اختبار إغلاق الفترة والسنة المالية، اختبار قيود الضرائب والعملات حسب بلد العميل، ومراجعة التقارير مع محاسب فعلي. أي وظيفة لم تجتز هذه الاختبارات ستبقى موسومة بأنها تجريبية ولن تُعرض كميزة إنتاجية.
